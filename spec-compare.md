# Spec — 多代號比較（Multi-code Comparison）

> NHI-drug-price-history 功能規格 **v0.3.2**（2026-09-14 定稿；2026-09-16 契約稽核與效能門檻修訂，見 §12）
> 上位文件：`spec.md`（資料模型與規則）、`.ai-review/plan.md` §5（驗收條件）
> 本文件只定義本功能；與上位文件衝突時以上位文件為準。
> 對應 §18 版本：`v0.3.0`
> **狀態：已通過兩輪覆審，可動工。** 第二輪無新 Blocker，架構決策全部確立；剩餘修正皆為契約細化，不需第三輪。

---

## 1. 目標與非目標

### 目標

在**使用者明示指定**的前提下，同時檢視 2–4 個健保代號的支付價歷史，回答：

1. 這幾個品項的支付價走勢與級距差異為何？
2. 誰在哪一年被調降、誰先終止支付？
3. 某一天（例如換藥討論當下）各品項分別是什麼狀態與價格？
4. 從各自最早有價紀錄到現在，相對變化幅度誰大？

### 非目標（明確排除）

| 排除項 | 依據 |
|---|---|
| 依相同成分／ATC／類似品名**自動**合併或自動帶入比較 | `spec.md` §17 Multiple products：不同 NHI code 代表不同 reimbursement item |
| 每 mg／每單位標準化比價 | §2 禁止用語；且 `規格量` 填寫率僅 47.4% |
| 宣稱任一品項「較便宜」「較划算」「可替代」 | 支付價 ≠ 採購價／自付額；等同性需另行定義 |
| 總支出、市占、申報量加權 | 無資料源（§9 NOT IN MVP） |
| 跨代號的價格平均、中位數、合計 | 同上；不同品項的價格聚合無臨床意義 |
| 比較集合的伺服器端儲存／分享短網址 | 純前端範圍外；以 URL 參數達成分享 |

**核心原則**：本功能只做「並列呈現」，一切等同性判斷留給使用者。

---

## 2. 資料層

**不新增任何 build 產物。** 全部由既有 `data/history/<prefix>.json` 與 `data/meta.json` 組成。

| 項目 | 規則 |
|---|---|
| shard 解析 | 依 `meta.shards.prefixLength` 與 `files` 決定檔名，前端不得寫死 4 碼。目前 `prefixLength = 4` 且所有 `files` 皆為 4 碼；**沿用既有 `engine.js` 的 `shardPrefix()`，不另寫「最長符合前綴」分支**（v0.1 的該說法暗示長度非均一，會產生永遠走不到的程式碼，plan-verdict-compare M5） |
| 重複 shard | 多個代號落在同一片時**只 fetch 一次**；以 in-memory Map 快取本次工作階段已載入的 shard |
| **併發去重** | 同一片的**進行中**請求也必須共享：第二個代號請求同片時，接上既有的 Promise，不得發第二個請求（v0.1 的「已載入 Map」只涵蓋已完成者，plan-verdict-compare H7） |
| **快取鍵與生命週期** | 快取鍵須含 `shardVersion`。快取命中時**仍須**比對 `meta.shards.versions[prefix]`；不符即視為失效、重新取得。**不得**混用不同 meta 批次的 shard |
| 失敗不進快取 | 載入失敗的 shard 不得寫入快取，必須可重試 |
| SW | 維持 `data/` 完全不經 Service Worker |
| 一致性檢核 | 每片的 `shardVersion` 必須等於 `meta.shards.versions[prefix]`；任一不符 → 不組合圖表與表格，提示「資料已更新，請重新整理」（§7 Data version） |
| 摘要推導 | 每個代號各自依 §5.5、以**瀏覽器本地日期 `T`** 為參考日期推導，與詳細頁使用同一組 `engine.js` 純函式 |
| `invalidRecords` | 不參與圖表、狀態 band 與統計；於摘要對照表以「該代號有 N 列日期異常」提示並連回詳細頁；**但必須出現在 §5.2 合併事件時間表與 CSV 中**，標「日期異常」並顯示原始值 |

> 所有摘要必須重用詳細頁既有的純函式。**不得**為比較頁另寫一套摘要邏輯，否則同一代號會出現兩個真相。

---

## 3. 選取模型（比較籃）

### 3.1 加入來源

| 入口 | 行為 |
|---|---|
| 搜尋結果每張卡片 | 「＋比較」按鈕 |
| 詳細頁標題列 | 「＋加入比較」按鈕 |
| 比較頁內的搜尋框 | 直接加入 |

**禁止**：任何自動加入。即使未來實作「同成分側欄」，該側欄的品項也必須由使用者逐一點擊加入。

### 3.2 容量與狀態

- 上限 **4 個代號**。已滿時「＋比較」呈 disabled 並提示「最多 4 個，請先移除」
- 下限 2 個才進入比較視圖；僅 1 個時按鈕提示「再加入 1 個品項即可比較」
- 重複代號不可加入（按鈕改為「已加入」）
- 比較籃為**畫面底部常駐列**（sticky），顯示已選代號 chip、移除鈕、「開始比較」「清空」
- 狀態存於 **sessionStorage**（非 localStorage）：比較是一次性任務，不是長期偏好；與搜尋頁「顯示已終止支付品項」的 localStorage 偏好不同
- 每個 chip 指派一個**固定序位**（1–4），決定其顏色與線型，移除後不重新洗牌（避免使用者記錯線）

### 3.3 URL 與 deep link

- `?codes=A017014321,AC48092100,AC48867100`（逗號分隔，大寫，依使用者加入順序）

**處理順序（八步，必須依序執行，不得調換）**（plan-verdict-compare M2、M3）：

> **代號格式**（v0.3.1 釘死）：清洗後須完全符合 `^[A-Z0-9]{10}$`。已對 `data/drug_index.json`
> 全量核實：45,179 個代號**全部**為 10 碼且僅含大寫英數，無例外。第 5 步之前必須已完成
> 第 4 步的去空白與轉大寫，因此格式檢查不接受小寫。

| 序 | 步驟 | 規則 |
|---|---|---|
| 1 | 讀取 | 同名參數重複出現 → 取**第一個**，其餘忽略 |
| 2 | **解碼** | 對整個參數值做 **URL decode 恰一次**；decode 失敗 → 整個參數視為空。**解碼在切分之前**，因此 `%2C` 解出的逗號**是分隔符** |
| 3 | 切分 | 以逗號切分；連續逗號與前後空白產生的空 token 直接丟棄，不計入略過數 |
| 4 | 清洗 | 去空白、轉大寫 |
| 5 | 格式檢查 | 不符 `^[A-Z0-9]{10}$`（清洗後）者標為「格式不合法」並保留，不靜默丟棄 |
| 6 | 去重 | 保留最先出現者 |
| 7 | 合併 `?code=` | 產生**完整有序清單**，此步**不截斷**（見下） |
| 8 | 截斷 | 超過 4 個 → 取前 4 個。`N = 完整清單長度 − 4`，提示「已略過 N 個超出上限的代號」 |

> v0.2 讓第 6 步「擠掉第 4 個」、第 7 步又把 N 定義為截斷數——四碼加一個 `code` 時第 8 步已無東西可截，N 變成 0（plan-verdict-r2 R5）。定案：**合併步只排序不截斷，截斷與計數統一在最後一步發生。**

**`?code=` 與 `?codes=` 並存**：`?codes=` 優先，`?code=` 的代號**無條件置於清單首位**：

| 情形 | 結果 |
|---|---|
| `code` 不在 `codes` 中 | 插入首位，清單長度 +1（是否超過 4 由第 8 步處理） |
| `code` 已在 `codes` 中（任何位置） | **移至首位**，不重複加入，長度不變 |

**名額佔用**：格式不合法與確認不存在的代號**佔用 4 個名額**（它們要顯示在畫面上）。「確認不存在」要載入後才知道，因此第 8 步只依格式層資訊截斷，不因載入結果重新截斷。

**代號狀態必須三分，不得混用**：

| 狀態 | 判定 | 顯示 |
|---|---|---|
| 格式不合法 | 第 4 步 | 「代號格式不正確」 |
| 確認不存在 | shard 載入**成功**但其中無此代號 | 「查無此代號」 |
| 載入失敗 | shard 404／網路錯誤／逾時／損毀 | 「資料載入失敗」＋重試，**不得**顯示為「查無此代號」 |

- 不存在與不合法的代號**保留在清單中**，不得靜默移除（否則分享出去的連結會少品項而無人察覺）
- **清單長度 ≥ 2（含異常項目）→ 一律停留在比較視圖**，異常項目依 §6.1 的矩陣呈現。這樣異常資訊才不會在轉頁時消失
- 清單長度 **1 個**：若它是有效代號 → 進入詳細頁（`?code=`）並提示「比較需要 2 個以上品項，已為你開啟單品項頁」；若它是異常項目 → 停留在比較視圖顯示該異常
- 清單長度 **0 個** → 顯示「代號皆無資料」，回到搜尋入口，URL 清空

> v0.2 的「有效碼剩 1 個就轉詳細頁」與「保留不合法／不存在碼」互相衝突：一個正常碼加一個不合法碼，轉頁後原本要求保留的異常項目就消失了（plan-verdict-r2 R5）。改以**清單長度**（含異常）為判準。

- **URL 一律優先於 sessionStorage**：URL 帶 `?codes=` 時完全以 URL 為準，不得把 session 裡的既有品項混入（否則分享者未選的品項會出現在對方畫面上）
- URL 無 `?codes=` 時才從 sessionStorage 還原
- 任何增刪都同步更新 URL（`history.replaceState`），不製造大量 history entry

§3.2 的「已滿時 `＋比較` 呈 disabled」是**手動加入**的規則；URL 正規化是另一個入口、另一套規則（截斷＋提示）。兩者適用範圍不同，並不矛盾。

---

## 4. 圖表規格

沿用 §8.2／§17 的一切限制（stepped、不得平滑、不得內插、不得把 null／終止／暫停當 0、不得延伸至 2910 年），另加下列多序列規則。

### 4.1 兩種 Y 軸模式

| 模式 | 定義 | 用途 | 限制 |
|---|---|---|---|
| **絕對金額**（預設） | 共用線性 Y 軸，單位 NTD | 看實際級距差異 | 級距差異大時小值品項會被壓平——可接受，這是事實 |
| **相對變化** | 各代號以**自己歷史首筆 `priced`** 為 100 指數化 | 看調降幅度節奏 | 見下方規則 |

**相對變化模式的規則**：

- 基準＝**該代號完整 history 中最早可唯一判定的 `priced` record**，與顯示區間無關。**切換 preset 只裁切視窗，不改變基準**
- 圖例固定列出每條線的基準日與基準金額
- 指數值 ＝ `100 × 當期價 ÷ 自身基準價`，四捨五入至小數 1 位（half away from zero）
- Y 軸標示「相對於各自基準＝100」，**不得**標示為「價格」或任何貨幣單位
- **禁止**將此模式描述為 normalized price comparison 或跨品項比價；它只呈現每個代號相對自身基準的變化

#### 4.1.1 基準候選資格與三種無線狀態

§4.3 序 1 禁止衝突列作為確定價格，因此「最早的 `priced`」在該列帶衝突時沒有唯一答案（plan-verdict-r2 R2）。定案：

- **候選資格**：`priceState = priced` 且該列**不帶** `conflicting_price_interval`、且其起日當天不存在其他有效紀錄
- 基準取候選中**最早**者。跳過不合格的更早列時，圖例的基準日顯示的是**實際採用那一列的起日**，**不得**把它稱為「歷史首筆價格」

圖例必須能表達三種不同狀態，文案不得合併：

| 狀態 | 條件 | 圖例文案 |
|---|---|---|
| 從未有價 | 完整 history 無任何 `priced` | 「無有價紀錄」 |
| **基準無法判定** | 有 `priced` 但**全部**不合候選資格 | 「有有價紀錄，但基準無法判定」 |
| 有基準、區間內無可繪點 | 基準存在但視窗內無合格繪製區間 | 「此區間無有價紀錄（基準 YYYY-MM-DD／X 元）」 |

#### 4.1.2 繪製資格（H2 原始案例在此堵住）

**繪製資格＝有效有價區間與顯示視窗「相交」**，不是「起日落在視窗內」。

> 固定基準解決了分母歧義與 preset 漂移，但**沒有**解決第一輪 H2 的原始案例（plan-verdict-r2 R3）：`A020296321` 最後一筆為 2014-07-01 起、`to: null`、20.50 元，持續有價至今，但「近 3 年」視窗內**沒有任何 `from` 落在其中**。只取「起日落在視窗內」的實作仍會錯誤地判成無線。
>
> v0.2 的修訂紀錄宣稱固定基準「一併解掉 H2」——**只對了一半**，必須加上本條。

配套規則：

- **基準日在視窗外**時：只在圖例列出基準，**不在視窗外增加圖點**
- **Y 軸取值集合**：依「視窗內所有**可見**序列的可繪製點」計算；已隱藏的序列**不納入** Y 軸範圍
- §4.4「只有未來紀錄 → 不畫主圖」僅適用於**所有選定代號皆如此**的情形；與有歷史的代號混合時照常畫圖，未來列依 §4.2 的預告樣式呈現

> v0.1 的基準是「顯示區間內自己第一筆 `priced`」，有兩個問題（plan-verdict-compare H2、H3）：
>
> 1. **「區間內第一筆」有兩種讀法，兩者答案不同。** 實測 `A020296321` 最後一筆為 2014-07-01 起、`to: null`、20.50 元，持續有價至今；但「近 3 年」preset 區間內**沒有任何 `from` 落在其中**。照「起日落在區間內」實作，會判成「此區間無有價紀錄」而不畫線——該藥明明近三年一直有給付價。
> 2. **與 §1 目標 4 不一致。** 目標 4 問的是「從各自最早有價紀錄到現在」，浮動基準回答的卻是別的問題，且同一個日期在不同 preset 下會有不同指數值。
>
> 改為固定基準後兩者同時解掉（2026-09-14 使用者選定）。代價是「看某段區間內的相對節奏」要自己換算——列為 §11 已知限制。

### 4.2 視覺編碼（色盲安全）

house style 要求色盲安全配色，且 4 條線不得只靠顏色區分：

| 序位 | 顏色 | 線型 | 事件 marker 形狀 |
|---|---|---|---|
| 1 | 色票 1 | 實線 | 圓 |
| 2 | 色票 2 | 長虛線 | 方 |
| 3 | 色票 3 | 短虛線 | 三角 |
| 4 | 色票 4 | 點虛線 | 菱 |

- 預告區間：沿用單品項規則（淡色／虛線＋「預告」標籤）。與序位線型衝突時，以**降低不透明度**表示預告，線型仍代表序位
- 圖例位於圖表上方，顯示：序位色塊／線型示意／代號／中文品名（截斷）／現行狀態
- 點擊圖例 → 該序列 toggle 顯示（不移出比較籃）

### 4.2.1 「選定」「可見」「篩選」是三件事

v0.1 把三者混為一談：§4.2 說圖例 toggle 隱藏序列、§5.2 說等同表格篩選、§4.5 又要求 crosshair 列出**所有**選定代號（plan-verdict-compare M4）。定義如下：

| 概念 | 由誰改變 | 影響範圍 |
|---|---|---|
| **選定集合** | 比較籃增刪、URL | 一切。摘要對照表、CSV、X 軸範圍、序位配置皆依此 |
| **可見性**（圖例 toggle） | 點擊圖例 | **只影響主圖線段與狀態 band 的繪製** |
| **表格篩選** | 合併事件時間表的代號篩選器 | **只影響 §5.2 的列** |

- 隱藏**不改變**選取集合、序位、`T`、§4.1 的基準、X 軸範圍或摘要對照表
- crosshair **保留已隱藏代號的那一列**，並標示「（已隱藏）」——隱藏是視覺操作，不是移除
- 可見性與表格篩選**彼此獨立**，各自有自己的控制項
- 全部隱藏 → 主圖顯示「所有序列皆已隱藏」，摘要與表格不受影響
- 序位在移除後**不重新洗牌**；空出的序位在下次加入時才重新使用

### 4.3 非有價區間的呈現（關鍵設計）

單品項頁以「標示區塊」呈現終止／暫停。**4 個代號同時畫區塊會互相覆蓋，不可沿用。**

改為：

- 圖表**主區只畫有價線段**，非有價區間一律線段中斷，不畫任何區塊
- 圖表**下方另加「狀態時間帶」**：每個代號一條細長 band，沿同一 X 軸
- band 的樣式必須以**紋理或明度**區分，不只顏色

**狀態判定分兩層**（v0.2 把三個正交維度塞進同一張表，既不互斥也不窮盡，plan-verdict-r2 R1）：

#### 第一層：代號取得狀態（先判，優先於一切）

**由上而下取第一個符合的規則**；「格式不合法」的代號**一律不發出請求**，因此不會停在
「未請求」或「載入中」（v0.3.1 釐清：原表未寫優先序，格式不合法者同時符合「未請求」）。

| 取得狀態 | 判定 | band 與 tooltip |
|---|---|---|
| 格式不合法 | §3.3 第 5 步（不發出請求） | 灰階，「代號格式不正確」 |
| 未請求 | 尚未開始載入 | 空白，無 tooltip |
| 載入中 | 請求進行中 | 骨架樣式，「載入中」 |
| 載入失敗 | shard 404／網路錯誤／逾時／JSON 損毀 | 錯誤樣式，「資料載入失敗」——**不得**顯示為空窗 |
| 確認不存在 | shard 載入**成功**但其中無此代號 | 灰階，「查無此代號」 |
| 成功但無有效紀錄 | `records = []`（可能仍有 `invalidRecords`） | 灰階，「無有效支付紀錄」；有 `invalidRecords` 時附「該代號有 N 列日期異常」 |
| **成功** | `records` 非空 | 進入第二層 |

#### 第二層：該日的日期／價格狀態（僅對取得成功者）

**由上而下取第一個符合的規則（互斥優先序）**：

| 序 | 條件 | tooltip |
|---|---|---|
| 1 | 該日有 ≥2 筆有效紀錄，**或**該日有效紀錄帶 `conflicting_price_interval` flag | 「來源紀錄衝突，無法判定單一支付價」 |
| 2 | 早於**有效區間聯集**的最早 `from` | 「尚未有紀錄」 |
| 3 | 晚於**有效區間聯集**的最大迄日（開放迄日則不適用） | 「本站資料未涵蓋此日期」 |
| 4 | 落在聯集範圍內但該日無有效紀錄（**空窗**） | 「此日期無支付紀錄」 |
| 5 | 該日唯一有效紀錄 `priceState = malformed` | 「資料格式異常（原始值：\<rawPrice\>）」 |
| 6 | 該日唯一有效紀錄 `priceState = missing` | 「來源無支付價資料」 |
| 7 | `priceState = terminated`，該列之前**從未有** `priced` | 「健保支付價 0 元（此前無有價紀錄）」，**不得出現「終止」字樣** |
| 8 | `priceState = terminated` | 「已終止支付（終止前 X 元）」 |
| 9 | `priceState = suspended` | 「暫停支付（來源標示「\<rawPrice\>」）」，X ＝ 暫停前金額 |
| 10 | `priceState = priced` | 金額＋該區間 from／to |

**關鍵定義**：

- **有效區間聯集**：該代號所有 `records` 的區間聯集（開放迄日視為延伸至右界）。序 2／3／4 一律依此判定，**不依最後一列的位置**——長區間包覆短區間時，「最後起日那筆」已結束不代表資料覆蓋已結束（與 `spec.md` §6.4 的累計最大迄日原則一致）
- **flag 的作用區間**：`conflicting_price_interval` 只作用於**帶該 flag 的那筆紀錄所涵蓋的日期**，不擴張到首筆前、空窗或整條 history
- **X（前價）** 一律取「該列之前最後一個 `priced` 金額」；null 時該段敘述省略，不得轉為 0
- `everPriced` 指「**該列之前**是否曾有 `priced`」，不是整個代號的布林值（plan-verdict-compare M9）

**視覺樣式**：序 1／5／6 可**共用同一種異常樣式**，但語意不得合併——「有紀錄但價格未知」不等於「無紀錄」。**11 個語意狀態不需要 11 種紋理。**

衝突列（序 1）**不得**被選為確定價格，也不得作為 §4.1 的指數基準（見 §4.1.1）。

### 4.4 X 軸

- 預設區間：**左界**＝所有選定代號**全部紀錄**中最早的 `from`；**右界**＝`max(T, 所有選定代號最後一筆預告的 from)`，再留視覺邊界使預告可見
- preset：近 3 年／近 5 年／近 10 年／全部。皆為**閉區間**，右界同上，左界為 `T` 減去對應年數（閏日以實際日曆計算）
- 開放迄日（`to: null`）畫至右界，**不得**延伸至 9991231
- 只有未來紀錄、或全部代號皆無有效日期列 → 不畫主圖，顯示「無可繪製的區間」並保留摘要對照表
- 單日資料（左右界相同）→ 以單點 marker 呈現，不畫線段

> v0.1 文字寫「已生效紀錄的聯集」，公式卻含 `max(今日, 最後一筆預告起日)`，兩者矛盾（plan-verdict-compare M1）。以上以公式為準：**預告要能看見，所以右界必須含預告**。

**時間基準 `T`**：

- 一次比較**共用同一個 `T`**，於進入比較視圖時取得一次，該次檢視內不跨午夜自動更新（沿用 `spec.md` §5.5）
- **時間區間 preset 不改變 `T`**：不改摘要的參考日期、不改 §6.6 描述欄位的選列日期、不改預告判定、不改 §4.1 的基準

### 4.5 互動

- 共用 X 軸 crosshair：hover 任一 X 位置 → 單一 tooltip 列出**所有**選定代號在該日期的狀態，缺一不可：

  | 該代號在該日 | tooltip 顯示 |
  |---|---|
  | 有價 | 金額＋該區間 from／to |
  | 終止支付 | 「已終止支付（終止前 X 元）」或首列 0 元例外文案 |
  | 暫停支付 | 「暫停支付（來源標示「-」）」 |
  | 空窗 | 「此日期無支付紀錄」 |
  | 早於首筆 | 「尚未有紀錄」 |
  | 來源衝突 | 「來源紀錄衝突，無法判定單一支付價」 |

- 觸控裝置：改為點擊定位 crosshair，再點空白處取消
- 點擊事件 marker → 捲動至下方合併時間表對應列並高亮

---

## 5. 表格規格

### 5.1 摘要對照表（並排欄）

每個代號一欄，列為指標。行動裝置改為橫向捲動，第一欄固定。

| 指標 | 來源 |
|---|---|
| 中文品名 / 英文品名 | §6.6 以 `T` 選列 |
| 成分 / 規格 / 劑型 / ATC / 藥商 | 同上 |
| 現行支付價（狀態） | §5.5 現行支付價規則 |
| 預告 | §5.5 預告標籤 |
| 最早可取得紀錄日期 | 已生效首筆 `from` |
| 歷史調價次數 | §5.5 |
| 最近一次調整 | §5.5 |
| 總變化 | §5.5 |
| 品質提示 | 代號層 `flags`＋`invalidRecords` 數 |
| — | `查看完整歷史 ↗`（連至 `?code=`） |

**禁止**：本表不得出現任何跨欄的合計、平均、差額或排名欄位。

### 5.2 合併事件時間表

所有選定代號的 records 依 `from` 合併排序，每列標示所屬代號（序位色塊＋代號）。

| 生效日 | **迄日** | 代號 | 品名 | 支付價 | 與前次差額 | 變動 % | 狀態 |

- 排序預設 newest first，可切換
- 欄位語意與 §8.3 完全一致（含「預告」「跨越停止期間」「前有空窗」「日期異常」標註）
- **必須納入 `invalidRecords`**，標「日期異常」並顯示原始值；異常日期**不得**被硬排成有效日期參與排序
- **排序位置**：所有 `invalidRecords` 一律置於**全表末尾**（不是「該代號區塊末尾」——全表依日期跨代號合併排序，日期交錯時根本不存在單一代號區塊，plan-verdict-r2 R7）；其內部以 `代號` asc → 原 `invalidRecords` 索引 asc 排序
- 有效列中，同一生效日的多列以 `代號` asc → 原 `records` 索引 asc 決定穩定順序
- 可依代號篩選（見 §4.2.1，與圖例可見性彼此獨立）
- **marker 目標被篩掉時**：點擊圖上 marker 而對應列已被表格篩選隱藏 → 顯示「該列已被目前的表格篩選隱藏」，**不自動解除篩選、也不得無反應**

> v0.1 的欄位表少了迄日，且 §2 只說「異常列以計數提示並連回詳細頁」，卻又宣稱本表「與 §8.3 完全一致」——三處互相矛盾（plan-verdict-compare M6）。以納入迄日與 `invalidRecords` 為準。

### 5.3 CSV 匯出

- 檔名：`nhi_compare_<code1>_<code2>[...]_<YYYY-MM-DD>.csv`
- 內容＝**合併事件時間表目前篩選後**的列，與畫面所見一致
- 首行加來源、參考日期 `T`、目前篩選條件與免責註記
- UTF-8 with BOM；`rawPrice` 逐字輸出，含逗號／引號／換行／中文時依 RFC 4180 跳脫
- **未完整成功時停用匯出**（見 §6）：任一選定代號載入失敗或版本不一致 → 匯出鈕 disabled，提示「部分品項尚未載入完成」

> v0.1 寫「內容＝完整列（不受畫面篩選影響時另備一個『匯出目前篩選』選項）」，該句語意破碎且同時承諾兩種輸出；又與 `spec-upcoming.md` §5.4 的「匯出目前篩選後」相反，同一產品兩套匯出慣例（plan-verdict-compare M14）。兩份規格統一為「匯出目前篩選結果」。
>
> CSV 沒有型別，BOM 與引號都不保證 Excel 保留 `0.00` 的尾隨零。驗收只斷言檔案位元組層，Excel 的型別推斷列為 §11 已知限制。

---

## 6. 載入、失敗與競態（沿用 §8.7）

多序列的關鍵差異：**部分失敗**。

| 情況 | 行為 |
|---|---|
| 任一 shard 載入中 | 圖表顯示骨架；**不得**先畫已到的序列再補上（會造成誤讀走勢） |
| 任一 shard 不可用（404／網路錯誤／逾時） | 圖表與表格**照常呈現成功的代號**，但頁首以明顯橫幅列出失敗代號＋重試鈕；圖例對應序位標「載入失敗」。禁止靜默略過 |
| 任一 shard 內容不合法（JSON 損毀／缺該代號） | 同上，訊息區分「資料損毀」與「查無此代號」 |
| `shardVersion` 與 `meta` 不一致 | **整頁不組合**（非部分降級），提示「資料已更新，請重新整理」 |
| `meta.json` 不可用 | 無法驗證版本 → 整頁不組合，不得略過版本檢查逕行呈現 |
| 使用者快速增刪代號 | 以請求序號丟棄過期回應；畫面只反映最後一次選取集合 |
| 重試進行中又再次點重試 | 沿用同一個進行中請求，不發第二個 |
| 重試進行中移除該失敗代號 | **移除該代號對該請求的需求**；橫幅移除該代號。**僅當該片已無其他有效需求時才取消請求**；同片仍有其他選定代號時請求必須繼續 |
| 舊請求晚到 | 依請求序號丟棄；不得覆蓋較新的畫面 |
| 從比較頁切回單品項 | 已載入 shard 沿用記憶體快取，不重新 fetch |
| 切回單品項後舊回應才到 | 丟棄，不得寫入單品項畫面 |
| 模組（動態 `import()`）載入失敗 | 顯示錯誤與重試；不得停在永久骨架 |

所有 fetch 依 `spec.md` §8.7 既有的 `TIMEOUT` 慣例設上限，不得無限等待。

> 「取消請求」與 §2 的「同片進行中請求必須共享」在 v0.2 是互相衝突的：兩碼共用同一片時，移除其中一個會取消另一個仍需要的請求（plan-verdict-r2 R6）。上表已改為**引用計數**語意——移除的是需求，不是請求。

### 6.1 輸出完整性矩陣

v0.1 只定義了頁首橫幅，沒定義**整份比較結果**在部分失敗時算什麼（plan-verdict-compare H4）。同一個選取集合，五種狀態的各輸出面行為：

**「完整成功」的定義**：**所有**選定代號皆已取得有效且版本一致的資料。任一代號處於其他取得狀態（§4.3 第一層）即非完整成功。

| 狀態 | 主圖／band | 摘要對照表 | 合併事件表 | crosshair | CSV 匯出 |
|---|---|---|---|---|---|
| 載入中 | 骨架 | 骨架 | 骨架 | 停用 | **停用** |
| 完整成功 | 全部**可見**序列 | 全部欄 | 全部列 | 全部代號 | 啟用 |
| **部分失敗** | 成功者的序列；失敗者序位留空 | 失敗代號的欄顯示「載入失敗」，**欄位不得消失** | 只含成功代號的列，**表頭明示「不含 N 個載入失敗的品項」** | 失敗代號**保留該列**並標「資料載入失敗」 | **停用** |
| **含確認不存在** | 該序位留空 | 該欄顯示「查無此代號」，**欄位不得消失** | 無該代號的列，表頭明示 | 保留該列並標「查無此代號」 | **停用** |
| **含格式不合法** | 該序位留空 | 該欄顯示「代號格式不正確」 | 同上 | 保留該列並標「代號格式不正確」 | **停用** |
| 全部失敗／全部不存在 | 錯誤狀態＋重試 | 同左 | 同左 | 停用 | 停用 |
| 混批（版本不一致） | 整頁不組合 | 整頁不組合 | 整頁不組合 | 停用 | 停用 |

> v0.2 的五態矩陣沒涵蓋 §3.3 新增的代號三分（plan-verdict-r2 R4）：「shard 載入成功但缺該碼」到底算完整成功（CSV 可略過）還是部分失敗（摘要卻要顯示錯誤的「載入失敗」），沒有答案。

**未知指標的呈現**：非成功代號的摘要欄**保留欄位並顯示取得狀態**，**不得填 0、不得留空白、不得沿用其他代號的資料**。這與 §5.1 的欄位定義相容——欄在，只是值改為狀態文字。

**「同一集合」的兩層含義**（M3 據此驗）：

| 層 | 定義 |
|---|---|
| **選定集合** | 比較籃／URL 決定。摘要對照表與 crosshair **必須**逐一交代這個集合的每一個代號，含異常者 |
| **資料子集** | 實際有有效資料、且通過可見性與篩選的部分。主圖、合併表、CSV 依此呈現 |

兩者不同是正常的；**不正常的是某個輸出靜默少了一個選定代號而不說明原因**。

**硬性規則**：

- **載入失敗不得被呈現為空窗或「查無此代號」。** 這是本功能最主要的靜默錯誤風險：使用者會以為看到的是完整比較
- 重試期間**保留已成功的結果**，只把失敗者的部分顯示為重試中；不得整頁退回骨架
- 重試成功後必須**實際補齊**該代號的資料，而不只是移除橫幅

---

## 7. 文案與免責

比較頁**頂部固定顯示**（不可摺疊）：

> 本系統顯示中央健康保險署公告之健保支付價，不代表醫療院所實際採購價、零售價或病人自付金額。
> 不同健保代號代表不同給付品項；並列呈現不表示品項可互相替代，亦不構成任何採購或換藥建議。

**禁止用語**（新增於 §2 既有清單）：「較便宜」「較貴」「性價比」「可替代」「等效」「建議改用」，以及任何跨品項的價格優劣敘述。

`spec.md` §17「描述欄位為來源以現況回填」的限制同樣適用：比較表的品名／藥商不得呈現為「該期間當時的」。

---

## 8. 效能目標

**量測起訖必須分三種情境，不可混為一談**（plan-verdict-compare H6、M7）：

| 情境 | 量測起訖 | 目標 |
|---|---|---|
| **A. 搜尋後進比較**（已完成啟動） | 點「開始比較」→ 圖表可互動 | **< 2,400 ms**（v0.3.2 修訂；原 600 ms，見 §8.1） |
| **B. 已快取重繪**（增刪一個已載入的代號） | 操作 → 重繪完成 | < 150 ms |
| **C. 冷開 deep link**（`?codes=` 直接載入） | 導覽開始 → 圖表可互動 | **不設門檻，僅記錄實測值** |

情境 C 不設門檻的理由：`app.js` 目前啟動要等 `status.json` ＋ `drug_index.json` ＋ `meta.json` 才 ready，而 `drug_index.json` 是 2.99 MB gzip、E7 實測「fetch → 解析完成」中位 4,634 ms。**本期不改啟動依賴，就不能承諾冷開 600 ms。** 這不等於重開已接受的 E7 優化工作。

| 其他項目 | 目標 |
|---|---|
| 首屏**比較專用資源**傳輸增加 | **0 bytes**——未進入比較視圖前不得載入比較專用的 JS／CSS |
| 首屏外殼增量（比較籃、搜尋卡「＋比較」鈕、共用 `engine.js` 增量） | 無門檻，但**須實測記錄** |
| 圖表 render | < 400 ms（單品項目標 300 ms 的放寬值） |
| 序列 toggle / 模式切換 | < 50 ms（僅重繪，不重算區段模型；區段模型須記憶化） |

### 8.1 門檻修訂（v0.3.2，2026-09-16）

情境 A 的 600 ms 是**在四片實際成本還沒量過時訂下的**。本節原文就寫明「最大片 0.123 MB gzip
**不能直接證明**四片 600 ms 可達，必須實測」——實測結果是不可達：

| 情境 | 原目標 | 實測（兩批各 3 次中位數） | 修訂後 | 餘裕 |
|---|---|---|---|---|
| A 搜尋後進比較 | 600 ms | **1,779／1,755 ms** | **2,400 ms** | 約 1.35×（高於觀測到的最差單次 1,911 ms） |
| B 已快取重繪 | 150 ms | 146／136 ms | **維持 150 ms** | 已達標，不放寬 |
| C 冷開 deep link | 不設門檻 | 7,263／7,393 ms | 維持不設門檻 | — |

**歸因**：瓶頸是 4 片 shard 的傳輸與 JSON 解析（指名四片合計 raw 5.19 MB／gzip 479 KB），
**不是圖表渲染**——情境 B（資料已在記憶體）只要 136 ms。479 KB 在 9 Mbps 下約 0.43 秒，
其餘為 CPU 4x throttle 下的 JSON 解析。

**這是接受現況，不是宣告已優化。** 要真的變快必須改變資料分片或抽取方式，那會動到既有的
shard 契約（`spec.md` §7），與 E7 的 `drug_index.json` 啟動成本是**同一個資料層天花板**，
應另行提案一併處理，不在本功能範圍內。

- 情境 B 已達標者**不放寬**：單次曾出現 159／168 ms，但契約定的是中位數
- 終點「可互動」的定義不得放寬：圖表已渲染**且 crosshair 實際可回應**（量測腳本派送
  mousemove 驗證 tooltip 真的列出所有代號，否則該次數據不算數）
- 日後若資料層策略改變，應一併重測本節並考慮把門檻收回

**量測條件**（比照 E7，寫入 `.ai-review/compare-acceptance.md`）：全量真實資料、CPU 4x throttle + Fast 4G、gzip level 6、冷快取與暖快取分別記錄、字型載入條件註明、每項 3 次取中位數。**指名 4 片不同 shard**（含最大片 `AC49.json`，1.41 MB raw／0.123 MB gzip）。

> v0.1 寫「首屏成本增加 0 bytes」，字面不可驗收：比較籃與搜尋卡入口本身就有成本，原生 ES module 也不會自動排除未使用的程式碼。
>
> 另：最大片 0.123 MB gzip **不能直接證明**四片 600 ms 可達，必須實測。

---

## 9. 驗收條件（M1–M17）

**每一條都必須能回答「什麼樣的實作會讓這條通過但功能其實是壞的」，並把那個弱化版本堵死。** v0.1 的 M1–M14 全數未通過這個檢驗（plan-verdict-compare §3）；以下為改寫版，每條附註所殺的弱化實作。

| # | 斷言 | 殺掉的弱化實作 | 測試位置 |
|---|---|---|---|
| **M1** | 比較頁與詳細頁**各自**符合獨立凍結的預期值，**再**斷言兩者相同。共同契約**不只摘要**，須涵蓋 §4.3 的日期狀態、band 區間、crosshair 文案、衝突排除與 §4.1.1 基準資格。測 2／3／4 碼與代號重排；含預告生效前／當日／後日、零筆有價、一筆有價、`metaVariants` 代號；**可見欄位歸屬須有實際視圖證據**，不能只驗模型 | 兩頁一起算錯、一起用錯的 `T`；摘要正確但 band／crosshair 用另一套日期判定；只比摘要物件抓不到畫面欄位接錯代號 | `tests-js/compare.test.mjs` |
| **M2** | 同片冷啟動且回應延遲時**恰一個請求**，且**每個代號都拿到各自完整且正確的資料**；快取再訪零新增請求；失敗後重試能重新載入。**必測「同片兩碼、重試中移除一碼」→ 請求繼續，另一碼仍正確取得**（§2 引用計數） | 只載第一個代號、第二個沿用第一個的內容——fetch 恰好一次，請求數測試全綠；或移除其中一碼時連帶取消另一碼仍需要的共享請求 | `e2e/compare.spec.mjs` |
| **M3** | 存在與不存在代號混合時，**主圖／band／摘要表／合併表／crosshair／CSV 六個輸出面交代的是同一個集合**。分測：無對應 shard、shard 有效但缺該碼、全部不存在。載入中不得先顯示「查無」；載入失敗不得判為不存在 | chip 保留不存在碼，但摘要或 CSV 靜默省略它，各輸出面集合不一致 | `e2e/compare.spec.mjs` |
| **M4** | 依 §3.3 的八步順序：**清洗→去重→合併→才截斷**。精確斷言最終集合、順序、略過數、畫面提示與更新後的 URL。含大小寫重複、空 token、連續逗號、未知碼、超量的組合 | 先截前 4 個再去重，使有效品項不足 4 個；或略過數固定寫 1 | `tests-js/compare.test.mjs` |
| **M5** | `?code=` 在 `codes` 的首／中／尾、清洗後才重複、容量 3／4／超量、空值、重複參數鍵——各情形的**精確**結果集合與順序 | 只處理「未滿且不重複」；已滿時偷丟一碼，或重複碼不移到首位 | `tests-js/compare.test.mjs` |
| **M6** | 失敗代號的**精確集合**出現在橫幅；成功代號的值正確；重試成功後**該代號資料實際補齊且無重複列**。另測全失敗、逾時、損毀 JSON、重試中移除該碼、舊回應晚到 | 橫幅出現但沒列全失敗碼；重試只移除橫幅、資料沒補 | `e2e/compare.spec.mjs` |
| **M7** | 在冷開、已有結果、快取命中、重試補片四個時機分別注入版本不符或缺版本 → **任何目前輸出（含摘要與 CSV）都不得呈現混批結果**，且保留明確的更新提示。一致且合法時須完整呈現 | 冷開時阻擋，但既有摘要／CSV 仍可用；只驗網路回應的版本，快取命中不驗 | `e2e/compare.spec.mjs` |
| **M8** | 逐代號逐區間斷言 band 的**起迄日、狀態與預告屬性**；價格線只覆蓋可判定的有價區間。分測閉區間邊界、同價續期、開放右界、`missing`／`malformed`／衝突，以及**巢狀區間**（長區間包覆短區間，驗「末筆後」依有效區間聯集而非最後一列）與**帶 flag 的作用範圍**（衝突只影響該筆涵蓋的日期，不擴張）。主圖非有價區間**既無區塊也無 0 值線** | 主圖沒有區塊，但非有價被畫成 0 線；band 四個圖例名稱俱全，實際區間或代號對錯 | `tests-js/compare.test.mjs` ＋截圖 |
| **M9** | `A020296321` 的首段（**1995-03-01～1998-02-28**）band tooltip **精確等於**「健保支付價 0 元（此前無有價紀錄）」，且該段**不得畫 0 價格線**。搭配「曾有價後終止」的正對照（`B009254100` 末段），其 tooltip 必須含「終止」。另加「首列 0 元續期」合成案例，驗判定**只看該列之前、不看未來** | tooltip 空白即不含「終止」；或把所有終止一律改稱 0 元，真正的終止被隱藏 | `tests-js/compare.test.mjs` |
| **M10** | 以**指名的真實 oracle** 斷言指數值（已用發布資料核實）：`A020296321` 基準 1998-03-01／34.00，末筆 20.50 → **60.3**；`AC48867100` 基準 2013-08-01／32.30，2026-04-01 起 12.50 → **38.7**。基準點恆為 `100.0`。**切換 preset 後基準與指數值皆不變**，只有可見範圍改變。另加十進位半位案例凍結「四捨五入至小數 1 位」的邊界。分測兩碼不同基準、切換往返、停止後恢復、預告區間 | 圖例基準日期改了但所有線仍用第一個代號當分母；或每次都把線直接畫成 100；或四捨五入邊界未凍結 | `tests-js/compare.test.mjs` |
| **M11** | §4.1.1 的**三種無線狀態**各自斷言精確文案：「無有價紀錄」／「有有價紀錄，但基準無法判定」／「此區間無有價紀錄（基準 …）」。無價碼與有價碼**同時**比較：前者無線但狀態 band 仍在，後者線正確。**必測 §4.1.2 的跨左界案例**：`A020296321` 選「近 3 年」→ 必須畫線（它 2014-07-01 起持續有價，但區間內無任何 `from`）。分測全停止、內部空窗、僅未來有價、全部序列皆無有價 | 任意不同文案即可滿足「文案不同」，卻分不出基準不可判定；或只取 `from` 落在視窗內的列，讓持續有價的代號錯誤無線 | `tests-js/compare.test.mjs` |
| **M12** | 除真實的 `BC23981100`、`A035680329` 外，**必加 `priced → gap → priced` 合成反例**（含兩側同價與異價兩組）：斷言空窗段既無水平線也無連接線，且兩側應有的線段仍存在 | 完全不處理空窗、只因終止／暫停就斷線——兩個真實代號照樣全綠（見 §9.1） | `tests-js/compare.test.mjs` |
| **M13** | 指定日期逐代號斷言**精確的**狀態、`rawPrice`、區間起迄與前價資訊。分測 §4.3 第二層 10 個序號**每一個**，含 `missing`／`malformed`；`suspended` 須分別以三種來源標記（`-`／`－`／`—`）驗 tooltip 顯示**實際 rawPrice** 而非寫死。另測轉換日前一天／當天／後一天、已隱藏、載入失敗。觸控點擊定位須得到相同結果 | 每個代號都有列，但全部複製同一價格；或任意位置固定附一句「此日期無支付紀錄」 | `e2e/compare.spec.mjs` |
| **M14** | **先**斷言必需欄位齊備且每個代號的資料完整正確，**再**斷言不存在任何跨代號算術（合計／平均／差額／排名）。同代號**自身**的歷次差額必須仍在且正確。含 tooltip 與 CSV | 表格完全空白就沒有合計；或把跨欄差額藏在別的欄名、tooltip 或 CSV 裡 | `e2e/compare.spec.mjs` |
| **M15** | **守恆，但三個輸出各有自己的分母**（見下）。每個輸出與其分母**逐筆對應**：內容相符、出現次數相同、無重複、無遺漏，且每一列都能唯一回指來源紀錄。含同起日多列與 `invalidRecords` | 重複一列、漏一列，列數檢查都抓不到 | `tests-js/compare.test.mjs` |
| **M16** | §6.1 矩陣**七態**逐格驗證（含「確認不存在」與「格式不合法」）× 六個輸出面，並驗**狀態轉移**（首次載入 → 部分失敗 → 重試 → 完整成功）。另驗「完整成功」時已隱藏的序列**不應**被畫出，且不影響摘要與 Y 軸 | 只做了橫幅，CSV 仍匯出不完整的「完整列」；五態全綠卻漏掉不存在／不合法／重試轉移；把「完整成功」誤讀為必須畫出隱藏線 | `e2e/compare.spec.mjs` |
| **M17** | §8 三情境實測並記錄，**每個目標各自明列終點**：「可互動」＝圖表已渲染且 crosshair 可回應，不是 JS 工作完成。含**首次**模式切換與**再次**切換（首次含模型建立，不得只報再次）。指名四片 shard 與四個代號；冷開 deep link 只記錄不判定 | 只量同步工作、預熱字型、用暖快取取得假通過；只報再次切換而首次超過 50 ms | `.ai-review/compare-acceptance.md` |

### 9.0 M15 的守恆分母

v0.2 的 M15 **數學上不可滿足**（plan-verdict-r2 R8）：它要求 records／表格／CSV／marker 四者出現次數全部相同且含 `invalidRecords`，但 §2 禁止 `invalidRecords` 進圖、§5.3 的 CSV 只含篩選後列、preset 與隱藏又會排除 marker。只要有一列日期異常，就同時要求它「不得進圖」與「必須有對應 marker」——正確實作永遠過不了，最後只會靠削弱測試或加誤導性圖點取得綠燈。

定案：**三個輸出各有自己的分母，不要求筆數天然相等。**

| 輸出 | 分母 |
|---|---|
| 合併事件表 | 通過**代號篩選**的 `records` ＋ `invalidRecords` |
| CSV | 匯出啟用時，**等於當前合併事件表的資料列** |
| 圖上 marker | 符合 §4.1.2 繪製資格、且序列**可見**的事件子集（`invalidRecords` 一律不在內） |

要驗的性質是**來源身分與內容不錯配**：每一列／每一個 marker 都能唯一回指一筆來源紀錄，且不重複、不遺漏該分母內的任何一筆。

### 9.1 golden 組合：v0.1 的覆蓋宣稱不成立

v0.1 宣稱 `AC48092100`／`AC48867100`／`B009254100`／`A020296321`「一次覆蓋四種 band 型態與 M9、M12」。實測形狀：

| 代號 | flags | priceState 序列 |
|---|---|---|
| `AC48092100` | `[]` | priced ×8 |
| `AC48867100` | `[]` | priced ×2, terminated, priced ×9（含 `relisted`） |
| `B009254100` | `[]` | priced ×7, suspended, terminated |
| `A020296321` | `[]` | **terminated**, priced ×5（首列 0 元） |

**四者 flags 全為空，無一帶 `gap`**，第四種 band（空窗）未被覆蓋，M12 不可能被這組驗到。

**更深的一層**：全 repo 只有 2 個 gap 代號（`meta.json` 的 `gapCodes: 2`），而兩者**都不是兩側皆 `priced`**——

| 代號 | 空窗區間 | 左鄰 | 右鄰 |
|---|---|---|---|
| `BC23981100` | 2019-12-01～2020-11-30 | `suspended` | `suspended` |
| `A035680329` | 2010-09-01～2010-09-30 | `priced` | `terminated` |

所以「完全不處理空窗、只因終止／暫停就斷線」的實作，在**兩個真實代號上都會綠**。真實資料與合成反例**不可互相替代**：前者驗 band／tooltip 的精確區間，後者（`priced → gap → priced`）才殺得掉跨空窗連線的缺陷。

**修訂後的建議組合**（皆已在 `tests/fixtures/` 有凍結快照）：

`A035680329`（多次降價＋空窗＋末列終止）／`AC48867100`（終止後恢復）／`B009254100`（有價→暫停→終止）／`A020296321`（首列 0 元）

覆蓋 priced／terminated／suspended／空窗四態 ＋ `relisted` ＋ 首列 0 元例外。**另須**合成 `priced → gap → priced`（M12）與「首列 0 元續期」（M9）。

### 9.2 `everPriced` 的定義必須釘死

`A020296321` 的首段是 `priceState: terminated` ＋ `eventType: **initial**`，其後有 5 筆 `priced`。因此：

- `everPriced = false` 指「**該列之前**從未有 `priced`」，**不是整個代號的布林值**——後者會讓 M9 直接跳過或反向
- 判定**只看排序在前的列**，不看未來的恢復支付
- 該段的事件是 `initial` 而非 `terminated`，所以 M9 **不能順便宣稱**覆蓋了 `terminated` 事件的所有文案分支

### 9.3 人工項目

**臨床藥師目檢**（比照 C2），情境須包含：

1. 4 代號完整畫面：確認無 §7 禁止用語
2. **移除一碼再重新加入**：確認序位與顏色不重新洗牌
3. **含預告的代號**：確認預告以不透明度表示、線型仍代表序位
4. **四條線在某段完全重合**：確認仍能透過圖例、band 與 tooltip 分辨
5. **手機觸控**：點擊定位 crosshair、再點空白處取消
6. 色盲模擬（deuteranopia／protanopia／tritanopia）下四條線仍可區分

禁止用語以人工判讀為準，**不以機械字串搜尋代替**語意檢查。

---

## 10. 實作順序（建議 commit 切分）

| 序 | commit | 內容 | 可獨立驗證 |
|---|---|---|---|
| 0 | `docs(spec): compare state and date contract` | **先定案** §4.3 兩層狀態表、§4.1.1 基準資格、§4.1.2 繪製資格、§4.4 共用日期範圍與 `T`、§6.1 輸出完整性矩陣。無程式碼 | **完成條件**：每張決策表皆無歧義（任一輸入恰好落入一列），且每個分支都寫得出確定的預期結果 |
| 1 | `feat(engine): per-code contract for compare` | 讓比較頁**重用**既有的 `summaryAt`／`selectMeta`／`stateLabel`／`chartModel`；只新增「組合各代號結果到共同座標」所需的純函式 | `npm test`（既有單品項測試須全綠；回歸證據見 §10.2） |
| 2 | `feat(ui): compare tray and url params` | 比較籃、sessionStorage、§3.3 八步清洗、M3–M5 | `npm test`＋`npm run e2e` |
| 3 | `feat(ui): multi-series chart` | 多序列 SVG、圖例、狀態 band、crosshair、M8、M9、M11–M13 | `npm run e2e` |
| 4 | `feat(ui): compare tables and csv` | 摘要對照表、合併事件時間表、匯出、M1、M14、M15 | `npm test` |
| 5 | `feat(ui): relative change mode` | 固定基準指數化與圖例基準標示、M10 | `npm test` |
| 6 | `feat(ui): compare failure matrix` | §6.1 各態、併發去重、快取版本檢核、M6、M7、M16 | `npm run e2e` |
| 7 | `chore: compare performance measurement` | M17，寫入 `.ai-review/compare-acceptance.md` | — |
| 8 | `chore: pharmacist review of compare view` | §9.3 六種情境的人工目檢與色盲模擬紀錄 | — |

### 10.1 真正的風險點是什麼

v0.1 說「第 1 步是本功能的真正風險點：若多序列模型不是從既有單品項函式重構而來，而是另寫一份，就會出現雙重真相」。

**這個判斷的方向對，但手段被過度指定**（plan-verdict-compare H5）。`engine.js` 已經把 `summaryAt`、`selectMeta`、`stateLabel`、`chartModel` 拆成獨立純函式；摘要重用**不以**「把單品項改成接受 N 組 records」為前提。強制先遷移單品項會擴大回歸範圍，還會把當時尚未定案的 band 與基準規則提前固化進介面。

因此第 1 步的通過條件改為：

- **單品項與比較頁共用同一套「每代號」判定規則，結果符合相同契約**
- **不**要求函式簽章從 1 組變成 N 組
- 比較層只負責：共同日期範圍、序位配置、排列與呈現
- 驗收證據見 §10.2

真正最難回頭的是**每代號的日期與未知狀態契約**（§4.3）、**選取集合與版本的一致性**（§2、§6.1），所以把它們提到第 0 步先定案。

### 10.2 第 1 步的回歸證據

v0.2 指定「以 `tests/fixtures/golden_frontend_2026-09-11.json` 做 before/after 位元組比對」，**對象是錯的**（plan-verdict-r2 R9）。已核實該檔頂層為 `{buildDate, index, shards}`——那是**凍結的輸入資料**，不是前端摘要或畫面輸出。拿它比 before/after，前端全部算錯也會完全相同；反過來若把「輸出位元組不變」解讀成包含內部模型序列化，合法重構只改欄位順序就會被誤拒。

定案：

| 角色 | 內容 |
|---|---|
| **凍結輸入** | `golden_frontend_2026-09-11.json`（維持現用途） |
| **固定參考日** | `T = 2026-09-11` |
| **獨立凍結的預期輸出** | 每個 golden 代號的摘要欄位、狀態 band 區間與狀態、事件標籤字串。**與待驗實作分開維護**，不得由待驗函式產生 |
| 通過條件 | 重構前後，上述**可觀察輸出**逐欄相同 |

位元組一致只用於**真正具有位元組契約的產物**（CSV），不套用於內部模型。

---

## 11. 已知限制（須寫入 README）

1. 比較對象完全由使用者指定；本站不判斷、不建議任何品項等同性或可替代性。
2. 相對變化模式的基準固定為各代號**歷史首筆有價**，不隨顯示區間改變。因此縮小區間只是裁切視窗，看到的仍是「相對於最早有價」的指數；若要看某段期間內的相對節奏，需自行以該段起點換算。
3. 絕對金額模式下，價格級距差異大的品項會在視覺上被壓平；這是共用線性軸的必然結果，非資料問題。
4. 上限 4 個代號為效能與可讀性取捨（4 條線已是色盲安全區分的實務上限）。
5. **冷開 `?codes=` deep link 仍須等待首頁索引載入完成**（`drug_index.json` 為 2.99 MB gzip）。本期不改啟動依賴，因此冷開的可互動時間只記錄實測值、不設門檻。
6. **CSV 的欄位型別由試算表軟體自行推斷**，本站無法控制。Excel 直接開啟可能把 `0.00` 顯示為 `0`。需要原值時請用「資料 → 從文字/CSV」匯入並將該欄指定為文字。檔案本身的位元組內容一律保真。

---

## 12. 修訂紀錄

| 版本 | 日期 | 修訂內容 | 依據 |
|---|---|---|---|
| v0.3.2 | 2026-09-16 | §8 情境 A 門檻依實測修訂（600 → 2,400 ms），新增 §8.1 記錄原目標、歸因與不放寬的項目 | `.ai-review/compare-acceptance.md` §1 |
| v0.3.1 | 2026-09-16 | 第 0 步契約稽核：釘死代號格式 `^[A-Z0-9]{10}$`（全量核實）、§3.3 統一稱八步、§4.3 第一層補上優先序（格式不合法不發請求）、§9 標題改為 M1–M17 | `.ai-review/compare-contract-audit.md` |
| v0.1 | 2026-09-14 | 初版 | — |
| v0.2 | 2026-09-14 | 第一輪覆審後修訂 | `.ai-review/plan-verdict-compare.md` |
| v0.3 | 2026-09-14 | 第二輪覆審後修訂 | `.ai-review/plan-verdict-r2.md` |

### v0.2 逐項

| 改動 | 依據 |
|---|---|
| §4.3：band 由「四態」改為完整狀態集合，補 `missing`／`malformed`／衝突／首筆前／末筆後／未載入；異常可共用視覺樣式但語意不得合併；衝突列不得作為確定價格或指數基準 | H1 |
| §4.1：相對變化模式基準由「顯示區間內首筆 priced」改為**固定的歷史首筆 priced**，preset 只裁切視窗（2026-09-14 使用者選定）。一併解掉「區間內第一筆」的兩種讀法歧義（實證 `A020296321` 近三年持續有價卻無 record 落在區間內）與「與目標 4 不一致」 | H2、H3、S2 |
| §6.1：新增輸出完整性矩陣（載入中／完整成功／部分失敗／全部失敗／混批 × 六個輸出面）；載入失敗不得偽裝成空窗或「查無此代號」；部分失敗時停用 CSV | H4 |
| §10 第 1 步：移除「必須把單品項改成接受 N 組 records」的門檻，改為「共用每代號判定規則、符合相同契約」；新增第 0 步先定案狀態與日期契約；新增以 `golden_frontend_2026-09-11.json` 做 before/after 位元組比對 | H5 |
| §8：效能量測拆為三情境（搜尋後進比較／已快取重繪／冷開 deep link）；冷開不設門檻只記錄（啟動仍須等 2.99 MB gzip 的 index）；0 bytes 精確限定為「比較專用資源」；指名量測條件與 shard | H6、M7 |
| §2：補併發去重（進行中請求共享）、快取鍵含 `shardVersion`、命中仍須比對 meta、失敗不進快取；`shardPrefix()` 沿用既有實作，不寫「最長符合前綴」的死分支 | H7、M5 |
| §4.4：修正「已生效紀錄聯集」與公式含預告右界的矛盾（以公式為準）；補 preset 閉區間、閏日、只有未來列、單日資料；明定一次比較共用同一個 `T`，preset 不改 `T`／描述選列／預告判定／基準 | M1 |
| §3.3：新增七步處理順序；`?code=` 合併改為無條件置首位（原正文與 M5 矛盾）；代號狀態三分（格式不合法／確認不存在／載入失敗）；補 0 碼與 1 碼的結果；明定 URL 優先於 sessionStorage | M2、M3 |
| §4.2.1：新增「選定／可見／表格篩選」三者的影響範圍定義；隱藏不改選取集合與基準；crosshair 保留已隱藏代號並標示 | M4 |
| §6：補逾時、重試中再重試、重試中移除、舊回應晚到、切回單品項後回應、`meta.json` 不可用、模組載入失敗 | M5（失敗終態） |
| §5.2：合併事件表補迄日欄與 `invalidRecords`；異常日期不得硬排成有效日期；明定同起日的穩定排序 | M6 |
| §5.3：CSV 統一為「匯出目前篩選結果」（與 `spec-upcoming.md` 對齊）；未完整成功時停用匯出；驗收改為位元組層，Excel 型別推斷列為已知限制 | M14 |
| §9：M1–M14 全面改寫為堵死弱化實作的斷言，每條附註所殺的版本；新增 M15（守恆）、M16（完整性矩陣）、M17（效能） | M8–M13、驗證策略全項 |
| §9.1：推翻 v0.1 的 golden 組合覆蓋宣稱（四碼 flags 全空、無一帶 gap）；並記錄更深一層的問題——全 repo 僅有的 2 個 gap 代號，空窗鄰居都不是 `priced`（`BC23981100` 兩側皆 suspended、`A035680329` 右側 terminated），因此**真實資料殺不掉「不處理空窗」的變異**，必須另加 `priced → gap → priced` 合成反例。建議組合改為 `A035680329`／`AC48867100`／`B009254100`／`A020296321` | M8、M10 |
| §9.2：`everPriced` 釘死為「該列之前從未有價」，不是整個代號的布林值（`A020296321` 首段事件是 `initial` 不是 `terminated`） | M9 |
| §9.3：人工項目擴充為六種情境（移除重加、預告線型、四線重合、手機觸控等）；禁止用語以人工判讀為準 | M12 |
| §10：實作順序依新增驗收重新切分為 9 步（含第 0 步定案） | 連動 |
| §11：已知限制新增第 2（固定基準的取捨）、第 5（冷開 deep link）、第 6（CSV 型別） | 連動 |

### v0.3 逐項（第二輪覆審後）

依 `.ai-review/plan-verdict-r2.md` 第二部分。**其中 2 項是 v0.2 修訂本身製造的新洞**（R8、R9）。

| 改動 | 依據 |
|---|---|
| §4.3 改為**兩層判定**：先判代號取得狀態（7 種），成功者才進入該日的日期／價格狀態（10 序互斥優先序）。v0.2 把三個正交維度塞進同一張表，既不互斥（帶衝突 flag 的 `priced` 同時符合兩列）也不窮盡（`records=[]` 但有 `invalidRecords` 無處可歸）。另定案「末筆後」依**有效區間聯集**判定而非最後一列位置（長區間包覆短區間會誤判），以及 flag 只作用於該筆涵蓋的日期 | R1（High） |
| §4.1.1 新增**基準候選資格**與三種無線狀態的區分文案。§4.3 禁止衝突列作確定價格，但「最早的 priced 恰好帶衝突」時 v0.2 沒有答案；「所有 priced 都衝突」這個狀態也無文案可用 | R2（High） |
| §4.1.2 新增**繪製資格＝區間與視窗相交**。**這是 v0.2 修訂紀錄的一處錯誤宣稱**：固定基準解決了分母歧義與 preset 漂移，但**沒有**解決第一輪 H2 的原始案例——`A020296321` 近三年持續有價卻無任何 `from` 落在區間內，只取「起日在視窗內」的實作仍會錯誤無線。另定案基準在視窗外時的處理與 Y 軸取值集合 | R3（Medium） |
| §6.1 矩陣補「確認不存在」與「格式不合法」兩列；定義「完整成功」＝所有選定碼皆取得有效且版本一致的資料；明定未知指標**不得填 0 或沿用他碼資料**；區分**選定集合**與**資料子集**兩層含義 | R4（High） |
| §3.3 處理順序改為 8 步：**解碼提前到切分之前且恰一次**（定案 `%2C` 是分隔符）；**合併步不截斷**，截斷與計數統一在最後一步（v0.2 的第 6 步擠掉第 4 個、第 7 步又算截斷數，四碼加一個 `code` 時 N 會變 0）；轉頁判準改為**清單長度含異常項目**，避免異常資訊在轉頁時消失 | R5（Medium） |
| §6「重試中移除該失敗代號」改為**引用計數**語意：移除的是需求，僅當該片無其他有效需求時才取消請求。v0.2 的這條與 §2 的「同片請求必須共享」互相衝突——兩碼共片時移除一個會取消另一個仍需要的請求 | R6（Medium，自造洞） |
| §5.2 `invalidRecords` 排序位置改為**全表末尾**（v0.2 寫「該代號區塊末尾」，但全表跨代號依日期合併，日期交錯時不存在單一代號區塊）；新增 marker 目標被篩掉時的明確行為 | R7（Medium） |
| §9.0 新增 **M15 的守恆分母**——表格／CSV／marker 各有自己的分母。v0.2 的 M15 **數學上不可滿足**：同時要求 `invalidRecords` 不得進圖、又要求所有輸出筆數相同且含它。正確實作永遠過不了，只會逼出削弱測試或誤導性圖點 | R8（Medium，自造洞） |
| §10.2 新增第 1 步的回歸證據定義。v0.2 指定的 before/after 位元組比對**對象是錯的**：已核實 `golden_frontend_2026-09-11.json` 頂層為 `{buildDate, index, shards}`，是**凍結輸入**不是前端輸出，前端全部算錯也會相同。改為「該檔當輸入 + 獨立凍結的預期可觀察輸出」；位元組比對只用於 CSV。第 0 步補完成條件 | R9（Medium，自造洞） |
| §9.1 正文「兩者的空窗鄰居都不是 `priced`」改為「兩者**都不是兩側皆** `priced`」——原句與同段自家表格矛盾（`A035680329` 左鄰就是 `priced`），結論不變 | Low |
| §9：M1／M2／M8／M10／M11／M13／M16／M17 依第二輪指出的弱化實作再次改寫。**M10 改用指名的真實 oracle**（已核實）：`A020296321` → 60.3、`AC48867100` → 38.7 | 驗證策略全項 |

Codex 第二輪明確判定 **M7、M9、M12、M14 無新問題**（第一輪指出的弱化版本已被堵死），並主動撤回兩項可能擴大範圍的建議（冷開 before/after 回歸門檻、改回完整事件 CSV）。

### 覆審歷程

| 輪次 | Codex thread | 原始輸出 | 判定 | 結果 |
|---|---|---|---|---|
| 1 | `01a09f02` | `plan-review-compare.md` | `plan-verdict-compare.md` | High 7、Medium 14；另 2 項待使用者裁決 |
| 2 | `01a09f1e` | `plan-review-compare-r2.md` | `plan-verdict-r2.md` | **無 Blocker**；High 3、Medium 6、Low 1 |
