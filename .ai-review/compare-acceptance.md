# 多代號比較 驗收紀錄（spec-compare.md §9）

> 2026-09-16／規格：`spec-compare.md` v0.3.1（第 0 步契約稽核：`.ai-review/compare-contract-audit.md`）
> 實作：`docs(spec): compare state and date contract` 起共 8 個 commit

## 1. M17 效能量測（§8）

**量測方式**：`node scripts/measure_compare.mjs`（非 CI）。全量真實資料、CPU 4x throttle、
9 Mbps／1.5 Mbps／60 ms、gzip level 6、字型照常載入、每項 3 次取中位數。
**指名 4 片不同 shard 與 4 個代號**（含最大片 `AC49.json`）：
`AC49155265`／`A049565335`／`AC48055100`／`A041725329`。

**「可互動」的終點**：圖表已渲染（DOM ＋ 強制 layout）**且 crosshair 實際可回應**——
腳本在收秒表後派送一次 mousemove，確認 tooltip 真的列出 4 列，否則該次數據不算數
（三次量測皆通過此檢查）。

| 情境 | 量測起訖 | 目標 | 實測（中位數） | 判定 |
|---|---|---|---|---|
| **A. 搜尋後進比較** | 點「開始比較」→ 圖表可互動 | < 600 ms | **1,779／1,755 ms**（兩批各 3 次的中位數；單次 1,745–1,911） | **未達標（約 3 倍）** |
| **B. 已快取重繪** | 移除一個已載入的代號 → 重繪完成 | < 150 ms | **146／136 ms**（單次 115–168） | ✅（單次 159／168 ms 超標，中位數達標） |
| **C. 冷開 deep link** | 導覽開始 → 圖表可互動 | 不設門檻，只記錄 | **7,263／7,393 ms**（單次 6,476–7,574） | 記錄 |

指名四片的實際大小（gzip level 6）：

| shard | raw | gzip |
|---|---|---|
| `AC49.json`（最大片） | 1.47 MB | 135 KB |
| `A049.json` | 1.29 MB | 113 KB |
| `AC48.json` | 1.22 MB | 116 KB |
| `A041.json` | 1.21 MB | 115 KB |
| 合計 | **5.19 MB** | **479 KB** |

- **A 未達標的原因是 shard 的下載與解析，不是圖表渲染**：情境 B（資料已在記憶體）只要 136–146 ms，
  兩者相減約 1.6 秒花在 4 片 shard 的傳輸與 JSON 解析上（合計 raw 5.19 MB／gzip 479 KB；
  479 KB 在 9 Mbps 下約 0.43 秒，其餘為 4x throttle 下的 JSON 解析）。
  §8 本來就寫明「最大片 0.123 MB gzip **不能直接證明**四片 600 ms 可達，必須實測」——實測結果是不可達。
- 處置需決策：(a) 接受現況並記錄（比照 E7 與預告中心 U14）；(b) 修訂門檻；
  (c) 改變資料分片或改為逐代號抽取（超出本規格範圍，會動到既有 shard 契約）。
- C 的 7.3 秒與 §11 已知限制 5 一致：冷開必須先等 `drug_index.json`（3.28 MB gzip）載入完成。
  本期不改啟動依賴，故只記錄。

| 其他項目 | 目標 | 實測 |
|---|---|---|
| 首屏比較專用資源 | 0 bytes | 比較頁與單品項頁共用同一份 `app.js`／`engine.js`，**未新增任何比較專用檔案**，故未進入比較視圖前不會載入比較專用資源 |
| 首屏外殼增量 | 無門檻，須記錄 | gzip **40,379 → 59,076 bytes（+18,697，+46.3%）**：index.html +151／styles.css +1,496／engine.js +5,233／app.js +11,817。相對首屏必載的 `drug_index.json`（3.28 MB gzip）約 0.6% |

## 2. M1–M16 逐條狀態

| # | 狀態 | 證據 |
|---|---|---|
| M1 | ✅ | `tests-js/compare.test.mjs`（比較頁與詳細頁共用同一組 engine 函式與同一個 `T`）＋`e2e/compare-tables.spec.mjs`「摘要欄位與詳細頁一致」「欄位歸屬正確」：以四碼實際視圖比對現行支付價、調價次數與最近一次調整字串 |
| M2 | ✅ | `e2e/compare-failure.spec.mjs`：同片冷啟動延遲時**恰一個請求**且兩碼各自正確；快取再訪零新增請求；**重試中移除同片一碼 → 請求繼續、留下的碼仍正確取得** |
| M3 | ✅ | 同檔：存在／不存在混合時，摘要欄、crosshair、狀態列交代**選定集合**（3 個），主圖／合併表／CSV 依**資料子集**並於表頭明示；載入中不得先顯示「查無」；載入失敗不得判為不存在 |
| M4 | ✅ | `tests-js/compare.test.mjs`：§3.3 八步逐步驗（清洗／去重在截斷前／解碼恰一次且在切分前／解碼失敗視為空／同名參數取第一個／略過數以完整清單計） |
| M5 | ✅ | 同檔 10 組 `?code=` 合併情形（首／中／尾／清洗後才重複／容量 3／4／超量／空值／只有 code），皆斷言精確集合與順序 |
| M6 | ✅ | `e2e/compare-failure.spec.mjs`：橫幅列出精確失敗集合；重試後資料**實際補齊且無重複列**；重試期間保留已成功結果、不整頁退回骨架；全部失敗的錯誤狀態 |
| M7 | ✅ | 同檔：冷開混批（shardVersion 不符／meta 缺該片版本）與**已有結果後才混批**，任何輸出（含摘要與 CSV）都不得呈現 |
| M8 | ✅ | `tests-js/compare.test.mjs` 以獨立凍結的預期逐段比對四個代號的 band 起迄／狀態／tooltip；巢狀區間依**有效區間聯集**、衝突 flag 不擴張；`e2e/compare-chart.spec.mjs` 另驗主圖非有價區間既無區塊也無 0 值線 |
| M9 | ✅ | 同兩檔：`A020296321` 首段 1995-03-01～1998-02-28 精確等於「健保支付價 0 元（此前無有價紀錄）」且不含「終止」；`B009254100` 末段為正對照；另有「首列 0 元續期」合成案例（判定只看該列之前） |
| M10 | ✅ | `tests-js`：基準與指數值對上規格指名的 oracle（`A020296321` 1998-03-01／34.00 → 60.3；`AC48867100` 2013-08-01／32.30 → 38.7），基準點恆為 100.0，preset 切換不改基準；十進位半位邊界以三組「浮點會少進一位」的案例凍結。`e2e/compare-chart.spec.mjs` 另驗畫面上的 `data-index` 與 Y 軸標示 |
| M11 | ✅ | `tests-js` ＋ `e2e`：三種無線狀態文案各自可分辨；**§4.1.2 跨左界案例**（`A020296321` 近 3 年內無任何 `from` 但持續有價 → 必須畫線）；基準候選排除衝突列 |
| M12 | ✅ | `tests-js`：`priced → gap → priced` 兩側同價與異價**兩組合成反例**，空窗段無線段；`e2e` 另驗真實 `A035680329` 的 2010-09 空窗無線段跨越 |
| M13 | ✅ | `tests-js`：§4.3 第二層 10 個序號各有案例，`suspended` 以三種來源標記分別驗 tooltip 顯示實際 `rawPrice`；`e2e`：crosshair 列出所有選定代號、觸控點擊定位與取消、同位置點擊與移動結果一致 |
| M14 | ✅ | `e2e/compare-tables.spec.mjs`：摘要表與 CSV 皆無合計／平均／排名等跨代號算術（含欄數檢查），同代號自身的歷次差額仍在且正確 |
| M15 | ✅ | 同檔：合併事件表的每一列都能唯一回指來源紀錄（`code`＋索引），與「records ＋ invalidRecords」逐筆對應、無重複無遺漏；CSV 的資料列等於當前表格列；篩選後兩者同步改變 |
| M16 | ✅ | `e2e/compare-failure.spec.mjs`：七態逐格（載入中／完整成功／部分失敗／確認不存在／格式不合法／全部失敗／混批）× 六個輸出面，並驗狀態轉移（首次 → 部分失敗 → 重試 → 完整成功）；完整成功時已隱藏序列不畫但不影響摘要、合併表與 CSV |
| M17 | **A 未達標** | 見第 1 節 |

**測試總數**：`uv run pytest -q` 245 ／ `npm test` 221 ／ `npm run e2e` 171，全綠。

## 3. §9.3 人工項目：臨床藥師目檢

**待執行**。環境：`node scripts/compare_review.mjs`

| 埠 | 內容 |
|---|---|
| **8768** | 真實四碼：`A035680329`（多次降價＋空窗＋末列終止）／`AC48867100`（終止後恢復）／`B009254100`（有價→暫停→終止）／`A020296321`（首列 0 元） |
| **8769** | 合成情境：四條線在 **2019 整年完全重合**（同為 20.00 元），第 4 碼另帶 2028 年的**預告區間**——這兩種情境在真實資料裡湊不出來 |

目檢項目：

1. 四代號完整畫面：無 §7 禁止用語（較便宜／較貴／性價比／可替代／等效／建議改用）
2. 移除一碼再重新加入：序位與顏色不重新洗牌
3. 含預告的代號：預告以不透明度表示、線型仍代表序位
4. 四條線完全重合時：仍能透過圖例、band 與 tooltip 分辨
5. 手機觸控：點擊定位 crosshair、再點空白處取消
6. 色盲模擬（deuteranopia／protanopia／tritanopia）下四條線仍可區分

禁止用語以**人工判讀**為準，不以機械字串搜尋代替語意檢查。

## 4. 規格偏離與決議

| 項目 | 處置 |
|---|---|
| 代號格式未定義 | v0.3.1 釘死 `^[A-Z0-9]{10}$`（全量核實 45,179 碼無例外） |
| §3.3 稱「七步」但表格 8 列 | 統一為八步 |
| §4.3 第一層缺優先序 | 補上「由上而下取第一個符合」，格式不合法不發出請求 |
| §9 標題 M1–M14 | 改為 M1–M17 |
| §10 把 shard 載入排在第 6 步 | 提前到第 2 步：沒有載入就沒有任何可驗的畫面；第 6 步改為專注在失敗矩陣與競態 |
| M17 情境 A 未達標 | 記錄未達標與歸因（瓶頸在 shard 傳輸與解析，非渲染），處置待決策 |

## 5. 待辦

- [ ] **M17 情境 A 未達標的處置決策**（接受現況／修訂門檻／改變分片策略）
- [ ] §9.3 臨床藥師目檢（6 項，含色盲模擬）
